using System.IO;

namespace Fred68.Tools.Utilita
	{
	interface ITextFile
		{
		#warning DA ABILITARE ! Poi completare il codice mancante
		// bool Scrivi(StreamWriter sw);
		// bool Leggi(StreamReader sr);
		}
	}